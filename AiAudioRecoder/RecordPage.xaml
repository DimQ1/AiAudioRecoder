<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:AiAudioRecoder.Converters"
             x:Class="AiAudioRecoder.RecordPage"
             Title="Records">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:DurationConverter x:Key="DurationConverter" />
            <converters:TranscriptionStatusConverter x:Key="TranscriptionStatusConverter" />
            <converters:StatusColorConverter x:Key="StatusColorConverter" />
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
            <converters:FilePathConverter x:Key="FilePathConverter" />
            <!-- Table header style added to ensure sticky header has consistent appearance -->
            <Style x:Key="TableHeaderStyle" TargetType="Label">
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="BackgroundColor" Value="LightBlue" />
                <Setter Property="Padding" Value="6,4" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
            </Style>
            <Style x:Key="TableCellStyle" TargetType="Label">
                <Setter Property="Padding" Value="6,4" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="LineBreakMode" Value="TailTruncation" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="20">
        
    <Border Grid.Row="0" BackgroundColor="LightGray" Padding="10" Margin="0,0,0,10" StrokeThickness="0">
            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                
                <VerticalStackLayout Grid.ColumnSpan="2" Margin="0,0,0,10">
                    <Label Text="Record Source:" FontAttributes="Bold" />
                    <!-- Changed: use FlexLayout to keep checkboxes in one line and wrap when needed -->
                    <FlexLayout Direction="Row" Wrap="Wrap" AlignItems="Center" JustifyContent="Start" Margin="0,5,0,10">
                        <HorizontalStackLayout Spacing="6" Margin="0,0,30,0">
                            <CheckBox x:Name="MicrophoneCheckBox" IsChecked="True" VerticalOptions="Center" />
                            <Label Text="Microphone" VerticalOptions="Center" />
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Spacing="6" Margin="0,0,30,0">
                            <CheckBox x:Name="SystemAudioCheckBox" IsChecked="False" VerticalOptions="Center" />
                            <Label Text="System Audio" VerticalOptions="Center" />
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Spacing="6" Margin="0,0,30,0">
                            <CheckBox x:Name="AutoTranscribeCheckBox" IsChecked="True" CheckedChanged="OnAutoTranscribeChanged" VerticalOptions="Center" />
                            <Label Text="Transcribe after" VerticalOptions="Center" />
                        </HorizontalStackLayout>
                    </FlexLayout>
                    
                    <Grid ColumnDefinitions="*" Margin="0,10,0,0">
                        <!-- Record button updated -->
                        <Button x:Name="RecordButton"
                                Grid.Column="0"
                                Text="Записать"
                                Clicked="OnRecordClicked"
                                BackgroundColor="Green"
                                TextColor="White" />
                    </Grid>
                </VerticalStackLayout>

                <Label Grid.Row="1" 
                       Grid.ColumnSpan="2"
                       x:Name="QueueStatusLabel"
                       Text="Queue: 0 | Active: 0"
                       FontSize="12"
                       TextColor="Gray"
                       HorizontalOptions="Center" />
                
            </Grid>
    </Border>

    <Border Grid.Row="1" BackgroundColor="White" Padding="10" Margin="0,0,0,10" StrokeThickness="0">
            <Grid ColumnDefinitions="*,Auto">
                <Entry x:Name="SearchEntry" 
                       Grid.Column="0"
                       Placeholder="Search content..."
                       TextChanged="OnSearchTextChanged"
					   TextColor="Black" 
					   PlaceholderColor="Gray"
					   BackgroundColor="White"
					   />
                
                <Button Grid.Column="1"
                        Text="Refresh"
                        Clicked="OnShowRecordsClicked"
                        BackgroundColor="Blue"
                        TextColor="White"
                        Margin="10,0,0,0" />
            </Grid>
    </Border>

        <ProgressBar x:Name="TranscriptionProgress"
                     Grid.Row="1"
                     IsVisible="False"
                     Progress="0"
                     Margin="0,10,0,0" />

        <Grid Grid.Row="2" RowDefinitions="Auto,*" Margin="0,0,0,10">
            <!-- Sticky header row: labels now clickable for sorting -->
            <Grid Grid.Row="0" BackgroundColor="LightBlue" RowDefinitions="40" ColumnDefinitions="120,120,120,200,150,150" ColumnSpacing="1" RowSpacing="1">
                <Label x:Name="DateHeader" Grid.Column="0" Text="Date" ClassId="Date" Style="{StaticResource TableHeaderStyle}">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnHeaderTapped" />
                    </Label.GestureRecognizers>
                </Label>
                <Label x:Name="DurationHeader" Grid.Column="1" Text="Duration" ClassId="Duration" Style="{StaticResource TableHeaderStyle}">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnHeaderTapped" />
                    </Label.GestureRecognizers>
                </Label>
                <Label x:Name="StatusHeader" Grid.Column="2" Text="Status" ClassId="Status" Style="{StaticResource TableHeaderStyle}">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnHeaderTapped" />
                    </Label.GestureRecognizers>
                </Label>
                <Label x:Name="SummaryHeader" Grid.Column="3" Text="Summary" ClassId="Summary" Style="{StaticResource TableHeaderStyle}">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnHeaderTapped" />
                    </Label.GestureRecognizers>
                </Label>
                <Label Grid.Column="4" Text="Action" Style="{StaticResource TableHeaderStyle}" />
                <Label x:Name="FileHeader" Grid.Column="5" Text="File" ClassId="File" Style="{StaticResource TableHeaderStyle}">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnHeaderTapped" />
                    </Label.GestureRecognizers>
                </Label>
            </Grid>
            <ScrollView Grid.Row="1">
                <CollectionView x:Name="RecordsView" ItemsSource="{Binding DisplayedRecords}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid RowDefinitions="40" ColumnDefinitions="120,120,120,200,150,150" ColumnSpacing="1" Padding="0">
                                <Label Grid.Column="0" Style="{StaticResource TableCellStyle}" Text="{Binding EndTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" />
                                <Label Grid.Column="1" Style="{StaticResource TableCellStyle}" Text="{Binding Duration, Converter={StaticResource DurationConverter}}" />
                                <Label Grid.Column="2" Style="{StaticResource TableCellStyle}" Text="{Binding IsTranscribed, Converter={StaticResource TranscriptionStatusConverter}}" TextColor="{Binding IsTranscribed, Converter={StaticResource StatusColorConverter}}" />
                                <Label Grid.Column="3" Style="{StaticResource TableCellStyle}" Text="{Binding Summary}" />
                                <HorizontalStackLayout Grid.Column="4" Spacing="4" HorizontalOptions="Center" VerticalOptions="Center">
                                    <Button Text="Transcribe" FontSize="10" Padding="4" Clicked="OnTranscribeClicked" IsEnabled="{Binding IsTranscribed, Converter={StaticResource InverseBoolConverter}}" />
                                    <Button Text="Delete" FontSize="10" Padding="4" BackgroundColor="Tomato" TextColor="White" Clicked="OnDeleteClicked" />
                                </HorizontalStackLayout>
                                <Label Grid.Column="5" Style="{StaticResource TableCellStyle}" Text="{Binding FilePath, Converter={StaticResource FilePathConverter}}" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </ScrollView>
        </Grid>

    <Border Grid.Row="3" BackgroundColor="LightGray" Padding="10" StrokeThickness="0">
            <Grid ColumnDefinitions="2*,*,*" ColumnSpacing="20">
                <Label Grid.Column="0" 
                       x:Name="QueueLabel"
                       Text="Queue: 0 tasks"
                       FontSize="12" 
                       TextColor="Gray" />
                
                <Label Grid.Column="1" 
                       x:Name="ActiveLabel"
                       Text="Active: 0 tasks"
                       FontSize="12" 
                       TextColor="Gray" />
                
                <Button Grid.Column="2" 
                        Text="Refresh"
                        Clicked="OnShowRecordsClicked"
                        BackgroundColor="Blue"
                        TextColor="White"
                        FontSize="12" />
            </Grid>
    </Border>

    </Grid>

</ContentPage>
